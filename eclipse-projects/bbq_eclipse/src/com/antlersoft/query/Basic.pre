package com.antlersoft.query;

import java.text.DateFormat;

import java.util.ArrayList;
import java.util.List;

import com.antlersoft.parser.*;


/**
 * A test class, since the basic vocab can't do much
 */
public class Basic extends BasicBase
{
    static private final ReservedScope reservedScope=new ReservedScope();
public Basic()
{
super( parseStates, new ParserEnvironmentImpl());
}
public ReservedScope getReservedScope() { return reservedScope; }

